{{- if and .Values.elasticsearchKibana.enabled (.Capabilities.APIVersions.Has "kyverno.io/v1") -}}
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: sync-logging-fluenbit-secret
  annotations:
    policies.kyverno.io/title: Sync fluentbit Registration Secret
    policies.kyverno.io/category: fluentbit
    policies.kyverno.io/subject: fluentbit Registration Secret
spec:
  generateExistingOnPolicyUpdate: true
  rules:
  - name: sync-logging-fluenbit-secret
    match:
      resources:
        kinds:
        - Namespace
        selector:
          matchLabels:
             kubernetes.io/metadata.name: "{{ .Release.Namespace }}"
    generate:
      kind: Secret
      name: logging-ek-es-elastic-user
      namespace: {{ .Release.Namespace }}
      synchronize: true
      clone:
        namespace: logging
        name: logging-ek-es-elastic-user
{{- end }}
